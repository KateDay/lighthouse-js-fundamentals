var packingList = ["bowls", "plates", "pots", "pans", "eating utensils", "glasses", "cups", "cooking utensils"];

console.log("Kitchen stuff to pack:");
for (var i = 0; i < packingList.length; i++) {
  console.log(packingList[i]);
}

/*this code works because we're delaring a counter variables called
i (short for index) that starts at 0 and increments by 1 at every step
until it matches the length of the array*/

packingList[3];
